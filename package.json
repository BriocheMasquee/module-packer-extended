{
  "name": "mpx",
  "private": true,
  "version": "0.18.0",
  "description": "Module Packer Extended \u2014 community rebuild of EncounterPlus Module Packer for the V5 module format.",
  "workspaces": [
    "core",
    "extension"
  ],
  "scripts": {
    "compile": "npm run compile --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  }
}
